/** Redis key polled by `/api/conflict/stream` (SSE). */
export const CONFLICT_UPDATES_KEY = 'conflict:last_update';

/** Semantic version for the on-device risk model (logistic blend). */
export const SAFETY_ML_MODEL_VERSION = 'pv-safety-v1';
